name = "icp-api"
main = "src/index.ts"
compatibility_date = "2024-10-01"
compatibility_flags = ["nodejs_compat"]

# Development vars (local wrangler dev)
[vars]
ENVIRONMENT = "development"
DATABASE_URL = "postgresql://neondb_owner:npg_XHrygVZb6e7c@ep-aged-field-ad3615sp-pooler.c-2.us-east-1.aws.neon.tech/neondb?sslmode=require"
SESSION_SECRET = "dev-secret-change-in-production-abc123xyz"

# Production env vars are set via `wrangler secret put` (not in this file)
# DATABASE_URL and SESSION_SECRET must be set as Cloudflare secrets
# R2 storage binding:
# [[r2_buckets]]
# binding = "STORAGE"
# bucket_name = "icp-storage"
